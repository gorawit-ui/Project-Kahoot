"use client";
import Link from "next/link";
import { FormEvent, useCallback, useEffect, useMemo, useState } from "react";
import { BonusDistributionGrid, ChoiceDistributionBoard, type BonusSummary, type ChoiceDistribution } from "@/components/reveal-distribution";
type RoomStatus = "lobby" | "question" | "reveal" | "paused" | "q20_summary" | "finished";
type HostState = { state: { room: { code: string; status: RoomStatus; currentPosition: number; deadlineAt?: string | null }; distribution?: ChoiceDistribution | null; bonusSummary?: BonusSummary | null }; playerCount: number; readyCount?: number; answerCount?: number; leaderboard: { nickname: string; score: number }[] };
const phase: Record<RoomStatus,string>={lobby:"กำลังรอเปิดประตู",question:"กำลังตอบคำถาม",reveal:"กำลังแสดงเฉลย",paused:"เกมพักอยู่",q20_summary:"กำลังสรุป Q20",finished:"เกมจบแล้ว"};
export default function HostDashboard(){const[room,setRoom]=useState("142426"),[data,setData]=useState<HostState|null>(null),[busy,setBusy]=useState(false),[showAll,setShowAll]=useState(false),[message,setMessage]=useState("กำลังเชื่อมต่อห้องจริง…"),[now,setNow]=useState(Date.now());const refresh=useCallback(async()=>{try{const r=await fetch(`/api/game/host/state?room=${room}&t=${Date.now()}`,{cache:"no-store"}),p=await r.json().catch(()=>({}));if(!r.ok){setData(null);setMessage("ยังไม่พบห้องจริง หรือ migration ยังไม่ครบ");return}setData(p);setMessage("เชื่อมต่อห้องจริงแล้ว · อัปเดตอัตโนมัติ")}catch{setMessage("การเชื่อมต่อสะดุด · กำลังลองใหม่")}},[room]);useEffect(()=>{void refresh();const p=setInterval(()=>void refresh(),1500),c=setInterval(()=>setNow(Date.now()),250);return()=>{clearInterval(p);clearInterval(c)}},[refresh]);const game=data?.state.room,position=game?.currentPosition??1,seconds=useMemo(()=>game?.deadlineAt?Math.max(0,Math.ceil((new Date(game.deadlineAt).getTime()-now)/1000)):0,[game?.deadlineAt,now]);async function prepare(e:FormEvent){e.preventDefault();setBusy(true);const r=await fetch("/api/game/setup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({room})}),p=await r.json().catch(()=>({}));setBusy(false);if(!r.ok){setMessage(`เตรียมห้องไม่สำเร็จ: ${p.error??"ตรวจ migration"}`);return}setRoom(p.room);setMessage(`เตรียมห้อง ${p.room} พร้อมแล้ว`)}async function command(status:RoomStatus,next=position){setBusy(true);const r=await fetch("/api/game/host",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({room,position:next,status})});setBusy(false);if(!r.ok){setMessage("สั่งเกมไม่สำเร็จ: สถานะห้องเปลี่ยนหรือ migration ยังไม่ครบ");return}void refresh()}function newRoom(){const n=String(Math.floor(100000+Math.random()*900000));setRoom(n);setData(null);setMessage(`สร้างรหัสใหม่ ${n} แล้ว · กดเตรียมห้อง`)}const board=game?.status==="reveal"&&position<20?<ChoiceDistributionBoard distribution={data?.state.distribution}/>:game?.status==="q20_summary"?<BonusDistributionGrid summary={data?.state.bonusSummary}/>:null;const rows=showAll?(data?.leaderboard??[]):(data?.leaderboard.slice(0,5)??[]);return <main className="page-shell"><section className="shell-content"><div className="topbar"><div><div className="eyebrow-small">HOST CONTROL · PRIVATE</div><h1 className="title">JIXGO Magical 24</h1></div><div className="topbar-actions"><Link className="back-link" href="/preview">ดู Preview</Link><Link className="back-link" href="/">หน้าแรก</Link></div></div><div className="host-grid"><div className="panel"><form className="form-stack" onSubmit={prepare}><label>รหัสห้อง<input className="code-input" value={room} onChange={e=>setRoom(e.target.value.replace(/\D/g,"").slice(0,6))} required/></label><div className="host-room-actions"><button className="button ghost" type="button" onClick={newRoom}>สุ่มรหัสห้องใหม่</button><button className="button ghost" type="submit" disabled={busy}>เตรียมห้อง / seed คำถาม</button></div></form><p className="answer-status">{message}</p><Monitor game={game} players={data?.playerCount??0} ready={data?.readyCount??0} answered={data?.answerCount??0} seconds={seconds}/>{board}<div className="control-grid"><button className="button primary" disabled={busy||!data||game?.status==="q20_summary"} onClick={()=>void command("question")}>{game?.status==="question"?"เริ่มเวลาใหม่":position===1?"เริ่มเกม":"เริ่มข้อนี้"}</button>{position<20?<button className="button ghost" disabled={busy||!data||game?.status!=="reveal"} onClick={()=>void command("question",position+1)}>ข้อต่อไป</button>:null}<button className="button ghost" disabled={busy||!data||game?.status!=="question"} onClick={()=>void command("reveal")}>{position===20?"แสดงเฉลย Q20":"แสดงเฉลยข้อนี้"}</button>{position===20&&game?.status==="reveal"?<button className="button primary" disabled={busy} onClick={()=>void command("q20_summary")}>แสดงสรุป Q20</button>:null}{position===20&&game?.status==="q20_summary"?<button className="button primary" disabled={busy} onClick={()=>void command("finished")}>แสดงผลรวมเกม ✦</button>:null}<button className="button ghost" disabled={busy||!data||position===20} onClick={()=>void command("paused")}>พักเกม</button></div></div><section className="panel"><div className="eyebrow-small">LIVE LEADERBOARD</div><h2>{showAll?"All players":"Top 5"}</h2><ol className="rank-list">{rows.map((x,i)=><li key={x.nickname}><span>#{i+1} {x.nickname}</span><strong>{x.score}</strong></li>)}</ol><button className="button ghost" onClick={()=>setShowAll(!showAll)}>{showAll?"แสดง Top 5":"แสดงทุกคน"}</button></section></div></section></main>}
function Monitor({game,players,ready,answered,seconds}:{game?:HostState["state"]["room"];players:number;ready:number;answered:number;seconds:number}){const s=game?.status;return <section className="host-monitor"><div className="host-monitor-head"><span>LIVE GAME MONITOR</span><strong className={`host-phase ${s??"offline"}`}>{s?phase[s]:"ยังไม่เชื่อมห้อง"}</strong></div><div className="host-monitor-main"><b>{game?`ข้อ ${String(game.currentPosition).padStart(2,"0")} / 20`:"—"}</b><em>{s==="question"?`${seconds} SEC`:s==="q20_summary"?"Q20 Summary":s==="reveal"?"Reveal":"Waiting"}</em></div><div className="host-monitor-stats"><span><b>{players}</b>คนในห้อง</span><span><b>{ready}</b>พร้อมแล้ว</span><span><b>{answered}</b>ส่งคำตอบ</span></div></section>}
